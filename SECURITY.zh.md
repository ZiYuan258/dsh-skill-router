# 安全政策

[English](SECURITY.md) | 中文

## 适用范围

本政策管的是 **`dsh-skill-router` 这个插件**：它的源码、发布的版本，以及挂在 release 上的产物。

**不覆盖**插件所读取的技能库。那是第三方内容，它们会指示模型做什么，责任在其作者，也需要**你自己**去审阅——见下面的[不可信内容才是真正的边界](#不可信内容才是真正的边界)。

## 报告漏洞

请用本仓库的 **GitHub 私密漏洞报告**：*Security* → *Report a vulnerability*。这样报告在修复发布前保持私密。

请说明你做了什么、发生了什么、你期望什么。一个针对夹具库的最小复现（`npm test` 会自建夹具）比一大段描述有用得多。

**没有 SLA，也没有赏金。** 这是个人项目、尽力而为地维护；能诚实承诺的是：报告会被读到、会被回复。

## 插件做什么——因此它不可能做什么

下面这些是对代码的事实陈述，所以政策是可核查的，而不是愿望：

| 属性 | 状态 | 由什么保证 |
|---|---|---|
| 执行技能里的代码 | **从不** | `host.js` 与 `client.js` 全文没有 `eval`、没有 `new Function`、没有子进程 |
| 跑 shell 或任何命令 | **从不** | 没有 `ctx.shell`、没有 `subprocess`、没有 `bash` |
| 发起网络请求 | **从不** | 两个半都没有 `ctx.web`、没有 `fetch`、没有 HTTP 客户端 |
| 写入、删除、重命名任何东西 | **从不** | `ctx.fs` 只调用 `resolve`、`stat`、`readText`、`listDir` |
| 安装或卸载技能 | **从不** | 它只报告路径；安装是 `dsh plugin` 或你自己的脚本的事 |
| 把数据发到任何地方 | **从不** | 没有任何东西离开进程；没有遥测 |
| 读取凭据或环境变量 | **从不** | 不访问 `process.env`；动态插件沙箱里连 `process` 都不存在 |
| 触碰页面 DOM | **仅两处，且可撤销** | `client.js` 只做 `document.createElement('style')` 与向 `head`／`documentElement`／`body` 之一 `appendChild`（卸载时移除该元素）。不查询、不修改产品自身的任何 DOM 节点 |
| 读取会话事件 | **只读，且只取标量** | 订阅会话账本并向后翻页（上限 200 页），只从 `tool/call` 事件上取标量叶子字段；不复制、不序列化、不外发 |

一句话：这个插件是**只读的索引查询 + 文件读取**，外加一个只读会话的客户端标签页。它带来的全部风险都来自**它读了哪些文件**，以及**模型随后拿这些内容做了什么**。

### 客户端半读什么

技能标签页的**全部**数据来自**会话账本**：注册项声明 `inject: (sessionId) => ...`，由会话作用域插槽把该会话的 `eventSource` 交给组件（`ctx.sessions.binding(sessionId)` 只在这个注入里调用一次，用来取绑定）。它从快照的 `entries` 里逐条挑出 `type === 'tool/call'` 的事件，只取**标量叶子字段**（`data.callId`、`data.name`、`data.turn`、`data.step` 与 `data.arguments` 里的技能名），用来判断"哪个加载工具被调用、技能叫什么"。它不复制事件、不序列化事件、不把会话内容发往任何地方——**也不进入模型上下文**。

它确实**订阅**了这条流（`subscribe`，按 400 ms 节流）并向历史方向**翻页**（`loadOlder()`，上限 200 页）：这两件事都只读不改，且订阅在插件卸载时解除。它同样不 import 任何包（React 由宿主提供），因此没有供应链可谈。

它保留在内存里的，只有一份**去重后的技能名清单**（`callId + 规范化技能名`），用来画那张表；会话正文的任何其他部分都不会被留存或复制。

## 不可信内容才是真正的边界

技能库是**不可信输入**。技能正文进入模型上下文后，模型可能把它当成指令——这正是 SKILL.md 存在的意义——所以恶意技能是一条提示注入通道，而含 `curl … | sh` 的技能是一个模型可能接受的"提议"。

插件的职责是**对来源保持透明**，而不是去沙箱化它无法判断的内容：

- 加载的正文被包在 `<skill_content name="…" source="…">` 里，与用户消息可区分。
- 每条命中与每次加载都报告 `source`（`library` 或 `resident`）、上游 `repo`、绝对 `path`。
- 工具卡片打印 base directory，所以技能引用的任何脚本都会先以一个可见路径出现，然后才可能被执行。

插件**刻意不做**的事：扫描技能正文里的危险模式，或拦截内容。需要那种保障的人应该自己去读文件；关键词扫描只会给出虚假的安全感，挡不住有决心的注入。对参考库（1025 个 `SKILL.md`）的实测：**3 个**在代码块里含 `curl … | sh`、**10 个**含 `rm -rf`、**50 个**含读环境变量、**0 个**含零宽字符或隐藏指令。这些不是库的缺陷——真实的工程技能库就是这样，这也正是**你**需要审阅自己要装什么的原因。

## 路径包含性

`skill_ref` 解析请求的路径，并在**任何 I/O 之前**拒绝技能目录之外的目标——该校验是对归一化路径的纯字符串运算，所以 `../` 穿越永远到不了文件系统。

- `resolvePath` 是导出的，并且被**直接单测**（`test/skill-ref.mjs`）：一条只通过工具间接验证的包含性规则，等于一条可能悄悄失效的规则。
- **已知局限**：包含性是**词法**的，不感知符号链接。若某个技能目录里存在指向库外的符号链接，读取会跟随该链接。参考库中符号链接数为 **0**，所以这目前是理论风险；写在这里而不是留作隐含。修法是在比较前先解析链接——如果你维护的库确实用了符号链接，值得做。

## 供应链

- **零依赖、零导入。** `host.js` 不 import 任何东西；发布包里没有 `node_modules`，也没有构建步骤。
- **不得把 DSH 宿主包声明为依赖。** 一旦 `dependencies` / `optionalDependencies` / `peerDependencies` 里出现 `@deepseek-ai/*`，`test/boot-safety.mjs` 就会让构建失败。这不是风格问题：钉一份副本正是宿主自身模块被遮蔽的成因，而被遮蔽的 `@deepseek-ai/dsh-tools` 曾经让整个宿主在启动时挂掉。
- **从 GitHub 分发，不走 registry。** 包是 `private: true`；发布就是 git tag + 附带的 tarball。安装可以钉到你选的 tag 或 commit，装进 profile 之前可以完整阅读——这个包小到能一口气审完。
- **CI 每次推送都在 Node 20/22/24 上跑全套测试**，且无安装步骤（没有东西可装），所以被测试的产物就是发布的源码本身。

## 未经审阅不会加入的东西

记在这里，是为了让将来的改动必须**明确反驳**它们，而不是作为"顺手加的功能"溜进来：

1. 执行技能里的任何内容；
2. 任何形式的网络访问；
3. 安装、删除或修改技能；
4. 把库内容发到本机之外的任何地方；
5. 把"扫描危险模式"当作安全保证。

其中 (4) 与 (5) 最容易被当作功能提出来。(5) 尤其会让插件**看起来**更安全，却完全不改变真正的信任决策。

## 支持的版本

只支持最新发布版。修复进 `main` 并打新 tag；旧 tag 不做回溯移植。
